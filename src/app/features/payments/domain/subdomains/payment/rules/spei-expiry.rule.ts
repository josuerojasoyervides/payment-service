/**
 * SPEI expiry rules (Mexican regulation).
 * Pure domain constant — no i18n, no throws.
 *
 * Typical SPEI transfer window per Banco de México.
 */

/** Default expiry window for SPEI transfers (hours). */
export const SPEI_DEFAULT_EXPIRY_HOURS = 72;
